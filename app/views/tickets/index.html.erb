<h1 class="font-bold text-4xl">Tickets</h1>

<div id="tickets" class="min-w-full">
  <%= render 'ticket', status: 'open', tickets: @tickets.open %>
  <%= render 'ticket', status: 'closed', tickets: @tickets.closed %>
</div>
